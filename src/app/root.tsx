export const metadata = {
  title: "Food Delivery App",
  description: "Onlayn ovqat yetkazib berish xizmati",
};

export default function Root({ children }: { children: React.ReactNode }) {
  return children;
}
